{
  "name": "GuiCapture",
  "version": "1.0.0",
  "description": "Area where the Graphical User Interface works",
  "main": "./GUI/Capture/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fix": "standard --fix",
    "serve": "live-server --port=5000 --host=localhost"
  },
  "author": "Williams Pesic",
  "license": "MIT",
  "dependencies": {
    "interactjs": "^1.10.19"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.2",
    "live-server": "*",
    "shelljs": "^0.8.5",
    "standard": "*"
  }
}