{
  "name": "interactjs",
  "version": "1.0.0",
  "description": "Area where i can test interactjs",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fix": "standard --fix",
    "serve": "live-server --port=4200 --host=localhost"
  },
  "author": "Williams Pesic",
  "license": "MIT",
  "dependencies": {
    "interactjs": "^1.10.19"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.2",
    "live-server": "*",
    "shelljs": "^0.8.5",
    "standard": "*"
  }
}
