{
  "name": "drawing-captcha",
  "version": "1.0.0",
  "description": "Parent Folder for the whole project",
  "scripts": {
    "setup": "concurrently \"cd ./Backend && npm install\" \"echo Setup has been installed execute npm run start to start server\"",
    "start": "concurrently \"cd ./Backend && node server.js\" \"cd ./Frontend && npm run serve\"",
    "test": "node testRunner.js "

  },
  "author": "Williams Pesic",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
